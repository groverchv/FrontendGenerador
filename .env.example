# URL base del backend para llamadas HTTP
# Si no se define, usará el mismo origin donde corre el front
VITE_API_BASE=https://backend-generador-full-stack.up.railway.app

# URL del websocket (opcional). Puede ser base o incluir /ws
# Ejemplos:
# VITE_WS_URL=http://localhost:8080
# VITE_WS_URL=http://localhost:8080/ws
# Si no se define, intentará con VITE_API_BASE y con window.location.origin
# VITE_WS_URL=
